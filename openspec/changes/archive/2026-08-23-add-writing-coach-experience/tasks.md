## 1. Lock the Desktop Experience Contracts

- [x] 1.1 Add failing focused tests for the typed `CoachPassageSnapshot`, per-generation mapped-issue, controller-state, fixed-session, and cross-generation suppression contracts, including exact UTF-16 boundaries and canonical descriptor-parameter identity; verify mapped-issue and suppression identities cannot be substituted and the new target fails before implementation.
- [x] 1.2 Implement the minimal pure experience types and canonical key helpers under the desktop learning boundary; verify the focused contract tests pass without importing Svelte, Tauri, persistence, network, model, APA Check, or export modules.
- [x] 1.3 Update the LT-03 dependency-boundary test only as needed to permit the sanctioned LT-04 desktop registration while continuing to reject forbidden imports inside the pure coach engine graph; verify the focused boundary test passes and still fails against a fixture containing a forbidden edge.

## 2. Extract Eligible Passages and Preserve Exact Source Maps

- [x] 2.1 Add failing extraction tests for authored paragraphs in body, abstract, appendix, lists, and blockquotes and for exclusion of title fields, headings, keywords, tables, figures, equations, generated references/labels, and chrome; verify the current structural allowlist protects the `citation` node and Tiptap StarterKit `link` mark, no identifier node/mark exists, and plain unmarked URL-like or identifier-like text remains authored prose with no recognizer or heuristic.
- [x] 2.2 Add failing UTF-16 mapping fixtures for surrogate pairs, combining marks, hard breaks, repeated text, boundary positions, and noncontiguous or invalid mappings; verify tests require exact original ProseMirror positions and whole-generation failure rather than normalization, search, or guessed ranges.
- [x] 2.3 Add failing citation fixtures proving rendered citation text uses document language, is preserved in the immutable passage, is tied to the citation plugin's environment version, and is fully protected while atom interiors remain unnavigable; verify both English and Spanish stale-rerender cases fail before implementation.
- [x] 2.4 Implement the pure ProseMirror passage extractor and source-map validator using node/ancestor eligibility, an explicit allowlist of sourced node/mark types present in the current schema, and the existing citation formatter/version; verify tasks 2.1–2.3 pass with no text-shape heuristic, document transaction, or DOM text read.
- [x] 2.5 Add failing adapter tests for per-passage engine calls, passage/document order, protected-span forwarding, observed-substring validation, and rejection of the complete generation when any returned issue cannot map exactly; verify the focused target fails before issue adaptation is implemented.
- [x] 2.6 Implement the deterministic engine adapter with document-language requests, passage-relative UTF-16 offsets, exact mapped ranges, and fail-closed publication; verify the focused extraction/adapter suite passes for all six categories.

## 3. Build the Essay-Scoped Controller with Bounded Scheduling

- [x] 3.1 Add fake-clock failing tests for idle-before-Study, immediate first analysis, 300 ms trailing debounce, 1,000 ms maximum wait under continuous edits, latest-snapshot consumption, and timer cleanup; verify the controller test target fails before scheduling is implemented.
- [x] 3.2 Add failing stale-work tests covering essay ID, revision, document language, citation/reference-environment version, `apa:external` citation refresh, snapshot ID, generation, extraction failure, essay switch, and teardown; verify English and Spanish pending/fixed passages invalidate and bounded reanalysis uses the current rendered citations.
- [x] 3.3 Implement the essay-scoped controller state machine and bounded scheduler, exposing `idle`, `analyzing`, `issues`, `no-current-issues`, and `unavailable-for-current-text`; verify tasks 3.1–3.2 pass with no analysis before first Study entry.
- [x] 3.4 Add failing fixed-session and ordering tests proving full-passage immutability, exact-issue emphasis data, document/engine ordering, previous/next wrapping only for multiple issues, explicit selection changes, and invalidation on source, language, reference/citation refresh, essay, or lifecycle changes; verify the focused target fails before session behavior is added.
- [x] 3.5 Implement fixed-session selection and deterministic navigation without editable answers, examples, rewrites, Apply actions, or insertion callbacks; verify the fixed-session tests pass and background analysis cannot replace the selected passage or question.

## 4. Map Editor Transactions and Ephemeral Actions

- [x] 4.1 Add failing pure mapping tests for untouched, shifted, touched, deleted, split, and noncontiguous issue ranges across ProseMirror transactions, including repeated source text; verify an earlier unrelated edit shifts a suppression range into the next revision while ambiguous and source-touching mappings are rejected instead of searched or guessed.
- [x] 4.2 Add failing suppression tests proving Dismiss and Not helpful create an identity separate from the mapped issue's generation, rematch only mapped range/text/category/message IDs/canonical parameters across unrelated revisions, reset on any source, descriptor, language, or citation-environment change, and clear on essay switch, teardown, and application-session end; verify the shifted-range case stays hidden and Not helpful has no event, log, storage, telemetry, or network callback.
- [x] 4.3 Implement the schema-free ProseMirror plugin and in-memory suppression mapping, composed through an EditorScreen-owned sibling controller and typed `Editor.svelte`/`createTesinaEditor` bridge without replacing or multiplexing `$tesina-editor-addon`; verify tasks 4.1–4.2 pass, spelling proof remains mounted, and editor schema, content, transaction history, and persisted JSON remain unchanged.
- [x] 4.4 Add failing exact-navigation tests for current-range selection, scroll/focus restoration, stale-source failure, preview/selection/source/essay/teardown cleanup, repeated prose, and an optional non-color-only transient decoration; verify no test permits document mutation or approximate selection.
- [x] 4.5 Implement `Edit this passage` revalidation and selection-only navigation through the sibling coach controller and schema-free editor plugin; verify exact navigation passes, stale navigation returns to Write with the stale status, the spelling alias is unchanged, and undo depth/content remain unchanged.

## 5. Localize and Render the Dedicated Study Workspace

- [x] 5.1 Add failing exhaustive adapter tests enumerating exactly the six engine categories and every explanation/question message ID with typed parameters in English and Spanish; verify missing Paraglide messages fail compilation or the focused contract target and `observedText` remains unchanged.
- [x] 5.2 Add the minimal English and Spanish Paraglide messages and implement the exhaustive UI-locale adapter; verify UI-locale changes rerender categories, explanations, questions, controls, states, and accessible names without reanalysis or source-passage changes.
- [x] 5.3 Add failing component tests for the explicit Write/Study control, Write-default behavior, dedicated Study replacement, fixed passage/question-led issue view, deterministic state views, separate APA/coach counts and terminology, and absence of spelling corrections, generated prose, quizzes, model controls, answer fields, and Apply actions.
- [x] 5.4 Implement the Study component and focused `EditorScreen.svelte` integration while keeping the editor mounted but inert and hidden from the accessibility tree in Study; verify the component tests pass and returning to Write preserves content, selection/history unless explicitly navigating, spelling, APA Check, preview, references, and export controls.
- [x] 5.5 Add failing accessibility tests for keyboard-only mode and issue flow, visible focus, one structurally deduplicated polite live region, no background focus movement, semantic source emphasis, and stale-source announcements; verify each interaction is named and operable in both UI locales.
- [x] 5.6 Implement the focus and announcement contract and responsive/reduced-motion styling; verify component/CSS tests cover 320 CSS-pixel width, long English and Spanish source text, no horizontal overflow or clipped actions, non-color-only states, and no nonessential motion or smooth scrolling under reduced motion.

## 6. Prove Lifecycle Cleanup and Non-Interference

- [x] 6.1 Add focused essay-switch and teardown integration tests proving timers, generations, sessions, suppressions, decorations, announcements, and focus effects cannot cross essay boundaries; verify switching during pending analysis leaves the next essay in a fresh Write/idle state.
- [x] 6.2 Add automated before/after non-interference fixtures with coach state present that compare persisted essay JSON, APA results, export eligibility, and representative sanctioned export bytes, plus spies proving enter/review/navigate/Dismiss/Not helpful/exit makes no persistence, autosave, backup, settings, local/session storage, Tauri, network, telemetry, model, APA, preview, export, schema, or editor-content call; verify the student's later ordinary edit remains the sole coach-related content change.
- [x] 6.3 Run automated English-UI/Spanish-document and Spanish-UI/English-document integration coverage for continuous typing, exact repeated-text navigation, citation refresh, essay switching, preview, APA Check, spelling, and export; verify Study remains deterministic, local-only, question-led, and non-authoritative without relying on manual proof.
- [x] 6.4 Manually verify only the behavior that requires real assistive technology or visual layout: keyboard/screen-reader flow, focus/live-region output, reduced motion, and the 320 CSS-pixel English/Spanish layout; record the [assistive-technology and overflow evidence](../../../../docs/evidence/lt-04-writing-coach-experience.md).

## 7. Final Quality, Product Metadata, and Review

- [x] 7.1 Run the Svelte MCP autofixer on every new or changed `.svelte` file until it reports no findings, then run the desktop Svelte check through `deno task check`; verify no Svelte, TypeScript, locale, or accessibility diagnostic remains.
- [x] 7.2 Synchronize the repository's required per-change version fields, CHANGELOG entry, and focused user documentation for the approved deterministic Study experience without promising model, quiz, telemetry, APA authority, or an unapproved release; verify all version assertions and documentation links agree.
- [x] 7.3 Run the focused writing-coach engine and experience targets, `deno task check`, `deno task test`, `deno fmt --check`, and `deno lint`, followed by `openspec validate add-writing-coach-experience --strict` and `git diff --check`; verify every applicable gate passes and classify any unrelated baseline failure separately.
- [x] 7.4 Request an independent review against the current branch head after all implementation, metadata, documentation, and generated files are final; verify every actionable finding is resolved or explicitly reported before handoff, with no review based on a stale head.
